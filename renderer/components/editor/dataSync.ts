// import { DocCollection, type BlockSnapshot, type Doc, Job } from '@blocksuite/store'
// import { ZipTransformer } from '@blocksuite/blocks'
import { provider } from './provider/provider'

// export const workspaces = new Map<string, DocCollection>()

export const exportSnapshot = async () => {
  // const ws = workspaces.get
  console.log(provider)
  //const job = new Job(provider)

  //const snapshot = await job.docToSnapshot(provider)

  // ZipTransformer.exportPages

  

  // return snapshot
}