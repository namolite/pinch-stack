const handleInspectElement = (event, args) => {
  // ... 处理 "inspect-element" 的逻辑
};

const handleAnotherEvent = (event, args) => {
  // ... 处理 "another-event" 的逻辑
};

module.exports = { handleInspectElement, handleAnotherEvent };